jQuery

$(선택자).index() : 선택자의 형제 위치 번호를 추출함

var abc = $(선택자).index();
선택자의 형제 위치 번호를 추출해서 abc변수에 넣음 (예 : 1번째 태그면 0, 2번째 태그면 1)


기아자동차
키프레임 애니메이션 + add클래스, remove클래스
(양이 2개 이상일 때)

트랜지션, 트랜지션 딜레이 + add클래스, remove클래스
(양이 2개 이하일 때 보통 권장)

트랜스레이트 x값 변경
오퍼시티 변경
트랜지션 입력